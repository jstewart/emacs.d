;; -*- mode: emacs-lisp -*-
;; History entries used for anything adaptive display.
(setq anything-c-adaptive-history (quote ((command ("gnus" ("gnus" . 1)) ("rinari-goto-partial" ("partial" . 1))))))
